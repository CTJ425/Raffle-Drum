apiVersion: v1
kind: Service
metadata:
  name: raffle-frontend-service
  namespace: raffle-system
spec:
  type: NodePort
  selector:
    app: raffle-frontend
  ports:
    - port: 80
      targetPort: 80
